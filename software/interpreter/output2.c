#include <stdio.h>

#define s1 0

#define s2 1

void start(void)
{
}
void repeatAlways(void)
{
}
int main(void) {
start();
set_servo(s1, 11));
set_servo(s1, -22));
set_servo(s2, 33));
set_servo(s2, -44));
exit(0);
while (1) {
repeatAlways();
}
return 0;
}